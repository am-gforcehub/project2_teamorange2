{{!-- NEED TO STORE ID SOMEWHERE ON THIS --}}

<div class="fh5co-project masonry-brick">
    <a href="/item/{{id}}">
        <img src="{{photoUrl}}" style="height:75%; width:75%;" alt="Free HTML5 by FreeHTML5.co">
        <h2>{{name}}</h2>
        <p>{{price}}</p>
    </a>
</div>